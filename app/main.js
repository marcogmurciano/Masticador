import "./../assets/styles/main.sass";






//////////////////////////////////////-----------BARRAS ALEATORIAS----------------/////////////////////////////////////////





const generateRandom = () => {
  return Math.floor(Math.random() * (90 - 20 + 1)) + 20;
};

const randomArray = () => {
  return [generateRandom(), generateRandom(), generateRandom()];
};
const arrayk = randomArray();
console.log(arrayk);

const arrayNames = [
  "Atragantamientos evitados",
  "Calorias ahorradas",
  "Tu vida se alarga en un",
];

const insertBars = () => {
  const barras = document.querySelector(".random_bars")

  if (barras == undefined) {
    
  } else {
    let html = "";
  let counter = 0;
  for (const data of arrayk) {
    html += `
        <div class="bar_container flex flex-col gap-2">
          <h3 class="text-white mt-2 ml-2">${arrayNames[counter]}</h3>
          <div class="bar bg-white rounded-full flex items-center">
            <div class="inner_bar  w${data}percent "></div>
            <h3 class="percent">${data}%</h3>
          </div>
        </div>    
        `;
    counter++;
  }

  barras.innerHTML = html;
  }
};
insertBars();


const menu = document.querySelector('.menui');
console.log(menu)
const desp = document.querySelector('.desplegable');

menu.addEventListener('click', () => {
  if (desp.classList.contains('hidden')) {
    desp.classList.remove('hidden');
  } else {
    desp.classList.add('hidden');
  }
});